# Glitter

Professional social rooms app (Next.js 15). Email accounts, unique usernames, lounge rooms with a shared TV browser, and inclusive picture-based avatars.

## Dev

```bash
npm install
npm run dev
```

Open http://localhost:3000

## Notes

- Fresh accounts start with **0 friends**
- Brainstorm remains on `main`; this product lives on `cursor/glitter-comms-b425`
- For a separate live URL, create a new GitHub repo + Vercel project
